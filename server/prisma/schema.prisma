// server/prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  // DO NOT put the url line here in Prisma 7
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}